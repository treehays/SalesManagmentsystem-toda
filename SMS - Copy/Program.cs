using System;
using SMS.menu;
// Connections
namespace SMS
{
    class Program
    {
        static void Main(string[] args)
        {
            MainMenu mainMenu = new MainMenu();
            mainMenu.AllMainMenu();







            // string a = "w";
            // string b = "a";
            // string c = a+b;
            // System.Console.WriteLine(a+b);

            // DateTime dv = DateTime.Parse("2022/11/29");
            // DateTime che = DateTime.UtcNow;
            // System.Console.WriteLine(DateTime.Now.Day);
            // System.Console.WriteLine(che);
            // // System.Console.WriteLine(che-dv.Day);
            // System.Console.WriteLine((dv-che).Days+1);
            // double cashTender1;
            // while (!double.TryParse(Console.ReadLine(), out cashTender1))
            // {
            //     System.Console.WriteLine("wrong input.. Try again.");
            // }
            // string x = "1212";
            // bool dd;
            // string wrd = x.ToString();
            // for (int i = wrd.Length-1; i >= 0; i--)
            // {

            // System.Console.Write(x.ToCharArray().Reverse());
            // Console.Write(new Random().Next(10000,99999).ToString());
            // var listOfNum = new List<string>();
            // var listOfNum2 = new List<string>();
            // for (int i = 0; i < 100; i++)
            // {
            //     listOfNum.Add(((Math.Abs((DateTime.Now.ToBinary()))).ToString()));
            //     listOfNum2.Add(((Math.Abs((DateTime.Now.ToBinary()))).ToString()).Remove(0, 1));
            // }
            // Console.WriteLine(listOfNum2.Distinct().Count());
            // foreach (var item in listOfNum2)
            // {
            //     Console.WriteLine(item);
            // }

        }
    }
}

